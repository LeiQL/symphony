# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/solution.symphony.symphony_versionedsolutions.yaml
- bases/federation.symphony_versionedcatalogs.yaml
- bases/federation.symphony_versionedcatalogs.yaml
- bases/fabric.symphony_versionedtargets.yaml
- bases/workflow.symphony_versionedcampaign1s.yaml
- bases/solution.symphony_versionedinstance1s.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_versionedsolutions.yaml
#- patches/webhook_in_versionedcatalogs.yaml
#- patches/webhook_in_versionedcatalog1s.yaml
#- patches/webhook_in_versionedtarget1s.yaml
#- patches/webhook_in_versionedcampaign1s.yaml
#- patches/webhook_in_versionedinstance1s.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_versionedsolutions.yaml
#- patches/cainjection_in_versionedcatalogs.yaml
#- patches/cainjection_in_versionedcatalog1s.yaml
#- patches/cainjection_in_versionedtarget1s.yaml
#- patches/cainjection_in_versionedcampaign1s.yaml
#- patches/cainjection_in_versionedinstance1s.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
